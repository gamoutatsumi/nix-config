{
  projectRootFile = "flake.nix";
  programs = {
    # keep-sorted start block=yes
    cue = {
      enable = true;
    };
    deno = {
      enable = true;
    };
    keep-sorted = {
      enable = true;
    };
    nixfmt = {
      enable = true;
    };
    stylua = {
      enable = true;
    };
    taplo = {
      enable = true;
    };
    yamlfmt = {
      enable = true;
    };
    # keep-sorted end
  };
}
